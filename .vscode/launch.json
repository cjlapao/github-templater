{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "GitHub Templater Debug",
      "type": "go",
      "request": "launch",
      "console": "internalConsole",
      "mode": "debug",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/cmd/github-templater/main.go",
      // Comment out if you do not need to setup environment variables for the module
      "envFile": "${workspaceFolder}/.env",
      "args": []
    }
  ]
}